import { Controller } from '@hotwired/stimulus';

export default class extends Controller
{
    onFormSubmitEnd() 
    {
        // var myModal = bootstrap.Modal.getOrCreateInstance(this.element);
        // myModal.hide();
        
    }
}